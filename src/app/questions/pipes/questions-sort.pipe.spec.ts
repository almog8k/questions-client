import { QuestionsSortPipe } from './questions-sort.pipe';

describe('QuestionsSortPipe', () => {
  it('create an instance', () => {
    const pipe = new QuestionsSortPipe();
    expect(pipe).toBeTruthy();
  });
});
